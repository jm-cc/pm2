application : {
	name : ring;
	flavor : mad4-mpi;
	networks : {
		include : networks.cfg;
		channels : ({
			name : channel_comm_world;
			net  : dalton_mx;
			hosts : (william, averell);
		});
	};
};
