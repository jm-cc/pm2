# Mad_cluster
# -----------

# Reseau LIP-ENS
cluster lip :
{
  hosts: muddy-waters, cuervo, singha;

  adapters:
    { alias: TCP; protocol : tcp; },
    { alias: MPI; protocol : mpi; };

  nfs: yes;
}
