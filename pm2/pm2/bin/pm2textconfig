#!/bin/sh

if [ -z "$PM2_ROOT" ]; then
	echo "Error: PM2_ROOT variable undefined!"
	exit 1
fi

case `basename "$0"` in
	*text*)
		exec pm2-config-flavor --text
		;;
	*x*)
		exec pm2-config-flavor --xdialog
		;;
	*menu*)
		exec pm2-config-flavor --dialog
		;;
	*)
		echo "Oups!"
		exit 1
		;;
esac

