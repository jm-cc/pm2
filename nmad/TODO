
TODO NewMad
-----------

* Supprimer les scheduler inutiles
  sched_mini/alt/null
[ ok ]

* Supprimer les protocoles inutiles
  proto_basic/so_basic/rdv
[ ok ]

* Supprimer p_pw->p_proto et p_core->p_proto_array
  utilisés uniquement par proto_basic, proto_rdv, etc.
[ ok ]

* Supprimer struct nm_gate_trk et p_pw->p_gtrk
  utilisé uniquement par sched_mini et proto_rdv
[ ?? j'avais fumé ? ]

* Statut de isend/recv_with_ref() ?
  complique pas mal sendrecv_interface, et utilisé par qui ?

* Fusionner core + sched_opt, supprimer struct nm_so_interface.

* Passer tout le code en PIOMan-only

* Revoir les conteneurs des gates et drivers
  (nb_* + *_array) pour la dynamicité

* Implémenter un cancel

* Interface synchrone de soumission de paquets

* jumbo tags maintenables

* info VP mask par gate

* remonter info taille effectivement reçue dans nm_sr_recv_success
