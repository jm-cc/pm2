application : {
	name : mad_mini;
	flavor : mad4;
	networks : {
		include : networks.cfg;
		channels : ({
			name : test;
			net  : dalton_mx;
			hosts : (averell, william);
		});
        };      
};
