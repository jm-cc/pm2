application : {
	name : mad_test;
	flavor : mad3;
	networks : {
		include : networks.cfg;
		channels : ({
			name : test;
			net  : solarnet_tcp;
			hosts : (tennessee, ravel);
		}, {
			name : control;
			net  : solarnet_tcp;
			hosts : (muddy-waters, debussy, ravel);
		});
		vchannels : {
			name : global;
			channels : (test, control);
		};
	};
};
