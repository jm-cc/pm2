application : {
  flavor : <name of the flavor>;
  networks : {
    include : <location of the network configuration file>;
    channels : (
      {
        name  : <name of channel 1>;
        net   : <identifier of the network for channel 1>;
        hosts : (<list of nodes for channel 1 separated by ,>);
      },
      ...
      {
        name  : <name of channel n>;
        net   : <identifier of the network for channel n>;
        hosts : (<list of nodes for channel n separated by ,>);
      }
    );
    vchannels : {
      name : <name of the virtual channel>;
      channels : (<list of physical channels for the virtual channel>);
    };
  };
};
