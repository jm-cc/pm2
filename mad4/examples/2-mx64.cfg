application : {
	name : mad_mini;
	flavor : mad4;
	networks : {
		include : networks.cfg;
		channels : ({
			name : test;
			net  : dalton64_mx;
			hosts : (billy, calamity);
		});
        };      
};
