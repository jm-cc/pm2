application : {
	name : mad_mini;
	flavor : mad3;
	networks : {
		include : networks.cfg;
		channels : ({
			name  : chan;
			net   : localnet_tcp;
			hosts : (localhost[0..2], localhost[3]);
		}, {
                        name  : chan2;
                        net   : localnet_tcp;
                        hosts : (localhost[0..2], localhost[3]);
                });
	};
};
