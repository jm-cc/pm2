enable libpthread generation (EXPERIMENTAL)
This produce a binary compatible libpthread using lpt_* versions of marcel
functions to export pthread_* function. This also export the other symbols
required by the standard libpthread.
This works only on Linux (x86 and ia64 for now)
