2004/05/12 Vincent Danjean
 * Suppression des délais d'attente dans leonie
 * Ajout du README.X pour expliquer comment faire pour éviter les erreurs
   avec xauth lors des forwards X11 de ssh en parallèle

2004/04/13 Vincent Danjean
 * nouvelle interface pour le polling
 * l'ancienne interface est encore présente pour le moment pour compatibilité
   sauf pour un point :
   // ATTENTION : changement d'interface
   // Remplacer : "FOREACH_POLL(id, _arg) { ..."
   // par : "FOREACH_POLL(id) { GET_ARG(id, _arg); ..."

