#!/bin/sh


# $1 = host name
# $2 = 'pm2' or 'mad2'

hostname=$1
mod=$2

for file in /tmp/${USER}-${mod}log-* ; do
	if [ -f $file ] ; then
		echo "*** Process `echo $file | sed -e 's/.*log-//'` on $hostname:"
		cat $file
	fi
done

