#!/bin/sh
###########
#
# Wrapper for pm2_xload

prog=$1
shift

if [ -n "${LEO_LD_PRELOAD}" ]; then
    env "LD_PRELOAD=${LEO_LD_PRELOAD}" $prog "$@"
else
#    echo $prog "$@"
    $prog "$@"
fi

echo "process terminated with code $?"
if [ "${PM2_PMODE}" = on ]; then
    sleep 2
    read dummy
fi
