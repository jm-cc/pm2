int
nm_ns_network_sampling(struct nm_drv *driver,
                       uint8_t gate_id,
                       int connect_flag);

double
nm_ns_evaluate(struct nm_drv *driver,
               int length);
